// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("daemon", "for daemon support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("daemon", "enable daemon support", "no");

if (PHP_BLOG != "no") {
	EXTENSION("daemon", "daemon.c");
}

